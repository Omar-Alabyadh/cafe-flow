/**
 * Dashboard sidebar placeholder.
 * It documents where navigation modules will be added later.
 */
export function SidebarPlaceholder() {
  return (
    <aside className="w-64 border-e border-border bg-muted p-4">
      <h2 className="text-sm font-semibold uppercase tracking-wide text-muted-foreground">
        Sidebar
      </h2>
      <p className="mt-3 text-sm text-muted-foreground">
        Dashboard navigation items will be implemented in later phases.
      </p>
    </aside>
  );
}

